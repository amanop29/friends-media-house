import { Contact } from '@/views/Contact';

export const metadata = {
  title: 'Contact Us',
  description: 'Get in touch with Friends Media House for your event photography needs.',
};

export default function ContactPage() {
  return <Contact />;
}
